export enum CometLockStatus {
  NotLocked = 'NotLocked',
  Locking = 'Locking',
  Expired = 'Expired',
  Migrate = 'Migrate',
}

export enum CometPoolType {
  NOT_MIGRATED = 0,
  MIGRATED = 1,
  DELEGATED = 2,
}

